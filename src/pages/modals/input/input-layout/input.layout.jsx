import { Layout, Title } from "../../styles";
import { InputContainer } from "../input-container";

export const InputLayout = ({}) => {
  return (
    <Layout>
      <Title>Input</Title>
      <InputContainer />
    </Layout>
  );
};
