import { ATTRIBUTE_NEGATIVE, ATTRIBUTE_PRIMARY } from "../button-utils";

export const handleClick = (attribute) => {
  if (attribute === ATTRIBUTE_PRIMARY) {
    alert("Primary button clicked!");
  } else if (attribute === ATTRIBUTE_NEGATIVE) {
    alert("Primary button clicked!");
  }
};
